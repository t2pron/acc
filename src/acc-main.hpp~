#ifndef OROCOS_ACC_COMPONENT_HPP
#define OROCOS_ACC_COMPONENT_HPP

#include <rtt/RTT.hpp>

class Acc : public RTT::TaskContext{
  public:
    Acc(std::string const& name);
    bool configureHook();
    bool startHook();
    void updateHook();
    void stopHook();
    void cleanupHook();
};
#endif
